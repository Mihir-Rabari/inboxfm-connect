InboxFM Connect is a headless AI automation platform focused on direct execution rather than visual workflows.

Core principles:
- Connection → Integration → Tool → Result
- No workflow builder
- No flow runtime
- Stateless execution
- MCP-first architecture
- Runtime contracts over visual graph models
- Evidence-driven refactoring and deletion
