# @inboxfm-connect/sdk

## 0.2.0

Initial Connect SDK with typed API contracts, retry and timeout handling, and project-scoped requests. Future entries are generated from SDK changesets by `bunx changeset version`.
